name: "Main"

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (GitHub)
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ github.token }}

      - name: Pre-build dev container image
        uses: devcontainers/ci@v0.3
        with:
          imageName: ghcr.io/${{ github.repository }}/devcontainer
          cacheFrom: ghcr.io/${{ github.repository }}/devcontainer
          push: always
          runCmd: pnpm restore

      - name: Lint
        uses: devcontainers/ci@v0.3
        with:
          cacheFrom: ghcr.io/${{ github.repository }}/devcontainer
          push: never
          runCmd: pnpm lint

      - name: Build
        uses: devcontainers/ci@v0.3
        env:
          DivertWindowsTests: "true"
        with:
          cacheFrom: ghcr.io/${{ github.repository }}/devcontainer
          push: never
          runCmd: pnpm build

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: |
            .local/windows/Divert.Windows.TestRunner
            .local/windows/Divert.Windows.Tests

  test:
    needs: build
    runs-on: windows-latest
    steps:
      - name: Download Build Artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts

      - name: Run Tests
        run: .local/windows/Divert.Windows.TestRunner/Divert.Windows.TestRunner.exe

      - name: Upload Test Results
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: .local/windows/TestResults

  code-coverage:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Download Test Results
        uses: actions/download-artifact@v4
        with:
          name: test-results

      - name: Codecov
        uses: codecov/codecov-action@v5
        with:
          files: .local/windows/TestResults/coverage.cobertura.xml
          token: ${{ secrets.CODECOV_TOKEN }}
